interface:
  display_name: "Publish Football2607"
  short_description: "Commit, push, open, and optionally merge a safe PR"
  default_prompt: "Use $publish-football2607 to validate and publish the current Football2607 changes, and merge the PR only when I explicitly request it."
